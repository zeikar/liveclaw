// Hiyori ships nine idle motions and the renderer plays them at random, so the stock model spends
// much of its time mid-gesture with its mouth open — restless for a character that sits on the
// desktop all day, and it leaves an open mouth meaning nothing in particular when lip sync is what
// it should mean. The .calm variant keeps the one idle motion that stays mouth-closed. The stock
// export sits untouched beside it, for a model picker to offer later.
export const LIVE2D_MODEL_PATH = 'live2d/Hiyori/Hiyori.calm.model3.json'
