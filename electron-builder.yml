appId: dev.zeikar.liveclaw
productName: LiveClaw
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
win:
  executableName: liveclaw
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  # Ad-hoc signed (no Apple Developer cert): identity "-" seals the whole bundle so
  # codesign validates and arm64 runs it (identity: null skips signing and leaves a
  # broken seal). Hardened runtime stays off so library validation doesn't reject the
  # Electron Framework. Downloaded builds still need a one-time quarantine bypass (see
  # README); switch to a Developer ID cert + notarize for the paid path.
  hardenedRuntime: false
  gatekeeperAssess: false
  identity: '-'
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    # Voice input is the only OS-level permission this app asks for; the main process denies every
    # other request, including the camera.
    - NSMicrophoneUsageDescription: LiveClaw uses the microphone for voice input to the character.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
